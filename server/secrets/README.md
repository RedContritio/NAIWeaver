This directory intentionally contains no real secrets.

Store production secrets outside the repository, for example:

`~/.config/naiweaver/novelai_token.txt`

Run the server with `NOVELAI_TOKEN_FILE` pointing at that external file.
